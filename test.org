* Link Test

This file exists purely to test how GitHub renders two different kinds of internal org links, side by side, on the same page. Push this to the repo, view it on GitHub, and click each link below to see which one actually jumps to the right section.

** Table of contents (CUSTOM_ID style)

- [[#sec-getting-started][Getting started]]
- [[#sec-features][Features]]
- [[#sec-sub-feature][A sub-feature]]
- [[#sec-notes][Notes]]

** Table of contents (fuzzy *Heading style, for comparison)

- [[*Getting started][Getting started]]
- [[*Features][Features]]
- [[*A sub-feature][A sub-feature]]
- [[*Notes][Notes]]

-----

* Getting started
  :PROPERTIES:
  :CUSTOM_ID: sec-getting-started
  :END:

If the CUSTOM_ID link above brought you here directly, that variant works on GitHub. If it instead tried to open something like ".../blob/main/*Getting%20started" as a separate page, it didn't.

This is a level-1 heading, to confirm CUSTOM_ID works at the top level, not just on nested sections.

* Features
  :PROPERTIES:
  :CUSTOM_ID: sec-features
  :END:

Some *bold text*, some /italic text/, and some ~inline code~, just to confirm ordinary formatting still renders normally alongside the CUSTOM_ID property drawer.

- A bullet list item
- Another bullet list item
  - A nested item, to confirm nesting still displays correctly

** A sub-feature
   :PROPERTIES:
   :CUSTOM_ID: sec-sub-feature
   :END:

This is a level-2 heading (nested under "Features"), to confirm CUSTOM_ID works the same way on a sub-heading, not just top-level ones.

| Column A | Column B |
|----------+----------|
| 1        | one      |
| 2        | two      |

* Notes
  :PROPERTIES:
  :CUSTOM_ID: sec-notes
  :END:

If only the CUSTOM_ID links work: that confirms the fix — switching this app's real README.org over to CUSTOM_ID-based links (instead of the current ~[[*Heading]]~ style) would make the table of contents actually usable on GitHub, at the cost of needing an explicit ~:CUSTOM_ID:~ property added to every heading.

If *neither* set of links works: something else is going on beyond the fuzzy-link limitation I found, and it's worth a second look before committing to this change.

If *both* work: no changes needed on GitHub's end at all, and the current syntax can stay as-is.
