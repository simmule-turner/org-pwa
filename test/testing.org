* Testing

org-pwa's engine code (~src/~) and browser-specific adapters (~src-browser/~) are unit tested with Node's built-in test runner, zero external dependencies:

#+BEGIN_SRC
node --test
#+END_SRC

This file is the detailed record of what that coverage actually includes — every real bug it caught along the way, not just a pass/fail count. See [[../README.org][README.org]] for what the app itself actually does; this file is purely about how it's verified.

** Coverage detail

1060 tests as of this writing, covering the parser (including a real, general bug this work caught: a line starting with ~*~ inside a ~#+BEGIN_...~ / ~#+END_...~ block was being incorrectly split off as an actual heading, with zero awareness of block context -- this affected any file with such a block, not just this documentation), every editing operation, fold/visibility logic (including the ~showall~ / ~showeverything~ distinction — confirmed against real org-mode's actual documented behavior: properties and block content stay folded in ~showall~, revealed only in ~showeverything~ — and a real bug this caught: ~isFullyExpanded~ never checked ~bodyHidden~ / ~drawersHidden~, so a heading loaded with either still hidden was incorrectly treated as already fully expanded), checkbox-cookie recalculation, search (plain and regex modes, including an invalid-pattern error path; matching against properties/TODO keyword/priority/SCHEDULED/DEADLINE, not just prose text; and filter-token parsing and integration -- \+tag/-tag/todo:/priority:/key:value, AND-combining with each other and with free text, tag/property inheritance from ancestor headings (multi-level, an inherited tag still respecting a -tag exclusion, a heading's own property value always winning over an inherited one, and both correctly defaulting to real org's own actual defaults -- tags on, properties off), and URL/time false-positive exclusions), agenda/repeater expansion (including week/day boundary alignment, SCHEDULED/DEADLINE carry-forward with delay-based early warning, commented/archived-heading exclusion, the date-independent TODO view, and ~org-contacts-anniversaries~ — property parsing (including the nil-year and unparseable-value cases), age calculation and the "(xx)" unknown-age display, per-occurrence expansion across a multi-year range, the trigger line activating the scan regardless of its own position in the file, and full ~buildAgendaItems~ integration including a custom ~org-contacts-birthday-property~ key), correct resolution of a file with multiple ~#+TODO:~ lines, capture-template expansion and insertion (all four types, OLP target resolution, the sequential-table-mutation bug this coverage originally caught, and a serious data-loss bug where editing a just-captured item could silently overwrite unrelated pre-existing content elsewhere in the same heading), heading move/promote/demote (including whole-subtree relocation, recursive level shifts, and every natural-boundary no-op case), priority setting/clearing/validation and its round-trip through serialization, horizontal-rule detection (including that it correctly interrupts a paragraph rather than being swallowed as literal text -- a real bug this coverage caught), inline sub/superscript parsing across all three ~org-use-sub-superscripts~ modes (including disambiguation from the pre-existing ~_underline_~ marker, which shares the same ~_~ character), timestamp building/delay parsing and plain-timestamp-in-title editing for the structured SCHEDULED/DEADLINE editor, Local Variables parsing, sync/conflict handling, and all three storage adapters (mocking ~fetch~ for GitHub/WebDAV so tests never touch the network, including ~list()~ for both -- GitHub's Contents-API array response, and WebDAV's hand-rolled PROPFIND multistatus XML parser covering cross-server namespace-prefix variation, percent-encoded filenames, and a real bug this coverage caught: self-reference detection for a subdirectory listing, which only worked correctly for the root before the fix), and the Markdown/HTML export converters (every inline formatting type, lists including nested/ordered/checkbox/description-list items, tables with a real org-syntax rule row correctly excluded rather than becoming a phantom row, blocks including a COMMENT block correctly dropped entirely, subtree-scope level normalization making the selected heading the new top level, HTML escaping specifically verified to neutralize a literal ~<script>~ tag rather than passing it through as real markup, and a real bug this coverage caught in the exporter's own escaping: several ordinary punctuation characters were being needlessly backslash-escaped in Markdown output, and a ~doneKeywords~-styling bug in the HTML exporter where every TODO keyword would have rendered in the same "active" color regardless of the file's own ~#+TODO:~ sequence). ~app.js~ itself (UI wiring) isn't unit tested — it has no logic that doesn't ultimately call into the tested engine — but is checked for syntax validity as part of every change, and the capture UI flow, the More-menu restructuring, the data-loss fix, the Docs view (rendering real converted content correctly, folding and internal link navigation both working, and confirming viewing it never touches whatever file is actually open elsewhere in the app), heading reordering, the action-menu stay-open/tap-to-close behavior, properties/block-content rendering across all four ~#+STARTUP:~ modes, archive-tree cycling (the swipe gesture correctly refusing to open an archived heading whether cascading from a parent or acting on it directly, matching real org-mode's unconditional rule -- a genuine bug fixed here, since the direct case previously opened the whole subtree -- while the chevron deliberately keeps working on an archived heading either way, this app's stand-in for Emacs's own separate force-open mechanisms that a touch UI has nowhere else to put), and the ~org-contacts-anniversaries~ agenda display (default and custom birthday-property configurations both verified), and the search panel (regex toggle, filter tokens combining correctly through real input events, property-match navigation correctly revealing drawers, and the invalid-regex error path) and the GitHub/WebDAV file browser (folder navigation, .org filtering, opening a file, the "type a path instead" fallback correctly falling all the way back to the normal button row rather than getting stuck on stale state -- a real bug this testing caught and fixed, and the listing-failure error path), the general editor (structured tags/priority/properties editing alongside SCHEDULED/DEADLINE, all six fields committing together on Save or discarding together on Cancel, verified including that Cancel genuinely discards everything and not just the timestamp fields), the ~org-archive-subtree~ move (same-file archiving under a target heading, cross-file archiving via a mocked GitHub backend with the archive file created fresh and correctly stamped, and the local-filesystem permission limitation producing a clear error with nothing lost from the current file), a real bug this coverage caught where the archived clone's stamped properties were correct but the ~:ARCHIVE:~ tag itself was never actually added -- meaning the Unarchive-button detection that depends on it would silently never have worked --, the restore/unarchive feature (the dynamic Archive/Unarchive button swap, the confirmation dialog naming the actual destination and the cancel case doing nothing, ~org-archive-confirm: nil~ skipping the dialog entirely, and the full restore transaction verified end to end: opening an archive file, tapping Unarchive, and confirming the original file is written with the restored heading correctly filed under its recorded ~ARCHIVE_OLPATH~ before the archived copy is removed), the restructured 6-column action menu and centered button grids at every menu size, and the keyboard shortcuts (focus navigation, fold-cycling, TODO-cycling, move/promote/demote, all correctly disabled while typing in a field, Ctrl/Cmd combinations never triggering a shortcut), file-link navigation (a headline-target and a text-search-target link each confirmed switching documents and landing on the correct heading, including one nested two levels deep with ancestors correctly expanded), and ~org-agenda-files~ (multi-file aggregation rendering immediately with the current file's own items and updating again once a configured file's async fetch resolves, the loading/error indicator, and the current-file-also-in-the-list dedup), and the File → Export flow (format choice, scope choice, the heading picker listing every heading regardless of fold state, a real bug this testing caught where the file menu never visually closed after a successful export since ~render()~ alone never touches that separate panel, and a real filename bug this testing caught where the source file's own extension wasn't stripped before appending the new one, producing "notes.org.md" instead of "notes.md") were additionally verified via DOM-stub integration tests exercising the real button/prompt/edit/insertion/fetch/swipe-gesture/keydown paths end to end, not just the underlying engine in isolation. The undo/redo history model (~src/undo-history.js~) and its line-diff engine (~src/text-diff.js~) are both unit tested in full — the history model specifically covering that a genuinely new edit after undoing discards the redo "future" while merely browsing the history list never discards anything, the exact distinction the whole feature depends on getting right — and the full undo/redo/History-panel flow was separately verified end to end through the real app: toggling a TODO state and undoing/redoing it correctly, opening the History panel and confirming its list and labels, the diff view showing the actual change, jumping directly to an arbitrary entry rather than stepping one at a time, a genuinely new edit after an undo correctly discarding the stale redo branch (down to confirming the entry count itself, not just a label), and history correctly resetting to empty on a freshly opened document rather than carrying over from whatever was open before.

** Progress logging (Layers 1 and 2) and the LOGBOOK-related parser fixes

The \#+TODO: parenthetical-syntax fix, org-log-done (Layer 1), and the per-keyword conditional-logging engine (Layer 2) are covered by a progressively-built test suite reflecting exactly how the feature itself was built: 18 state-transition-matrix tests for the CLOSED-planning-line decision alone, then a further 36 covering the general per-keyword engine (~parseLogSpec~, ~effectiveLogSpec~, and ~decideLogbookEntry~'s conditional leaving-log rule specifically) -- including the exact worked example from the original specification (~TODO(t) WAIT(w@/!) | DONE(d!) KILL(k@)~) reproduced as literal test cases, not just described: WAIT to DONE correctly producing no redundant second entry since DONE already logs on its own entry, and WAIT to TODO correctly firing the leaving-log since TODO has no logging of its own -- verified twice, once at the pure decision-engine level and again end to end through the real app (a heading actually cycled through WAIT and DONE via the real TODO badge, confirming the LOGBOOK drawer ends up with exactly the entries real org itself would produce, note text and all). ~src/logbook.js~'s own parser is covered separately -- every real shape org's own default log-note format actually produces (state changes with and without a "from" clause, multi-line note continuations via the trailing-backslash convention, CLOCK lines recognized structurally though not acted on), plus round-trip safety specifically: an unrecognized LOGBOOK line a future version of this parser doesn't understand yet is still preserved verbatim, never dropped.

Two real, general parser bugs this work found while writing README.org's own documentation for the feature, independent of progress logging itself: a blank line between a list item and its nested sub-list (an extremely common, legitimate org style this app's own README uses throughout) was causing the sub-list to be parsed as a separate, un-nested sibling instead of a real child -- confirmed to affect the main outline's own rendering identically to the read-only Docs view, not a Docs-specific bug, and fixed with its own dedicated test coverage including three-level-deep nesting and the exact mixed ordered/unordered case that surfaced it. And a "::" character appearing inside a ~code~ or =verbatim= span (e.g. a list item whose own prose demonstrates description-list syntax, "(~term :: description~)") was being matched as if it were that item's own real tag separator, corrupting the item -- fixed with proper span-boundary awareness rather than a one-off workaround, verified a legitimate tag separator appearing AFTER such a literal-span example is still correctly found.

Real org's own border-character rules for inline markup (documented in this app's own README.org, under "Inline text markup") were also verified systematically against the actual README.org content itself, not just unit-tested in the abstract: a full-file automated scan checked every single ~...~ span's opening and closing characters against the documented valid-border sets, catching 34 separate occurrences of adjacent code spans concatenated with a bare "/" (invalid on both sides of the border) plus several more one-off violations (a trailing space before a closing tilde, an opening tilde immediately preceded by "/"), cross-checked against the actual rendered Docs-view output down to a final count of exactly the intentional literal-tilde cases (verbatim-wrapped syntax demonstrations, and real home-directory-shorthand paths in file: link examples) with zero unintended ones remaining.

** Progress logging (Layer 1) and the #+TODO: parenthetical-syntax fix

A real, general parsing bug this work found and fixed: ~#+TODO:~ lines using fast-access keys or logging specs (e.g. "TODO(t) WAIT(w@/!) | DONE(d!) KILL(k@)", or even just plain "TODO(t) | DONE(d)" with no logging at all) were never having the parenthetical suffix stripped before matching against a heading's own TODO keyword -- meaning any heading using such a keyword was silently invisible to TODO-state recognition everywhere in the app (no badge, absent from the TODO view and Agenda), for what is extremely common real-world ~#+TODO:~ syntax. Fixed with a new shared ~parseTodoSpecValue~ in org-parser.js, used by both the parser's own heading matching and todo-cycle.js's sequence resolution (previously two independent, and previously-already-once-buggy-for-this-exact-reason, implementations) -- covering every parenthetical combination, multi-line ~#+TODO:~ merging of the new key/logging metadata, and round-trip safety (the ~#+TODO:~ line itself is preserved completely verbatim regardless of how it's parsed for matching).

~org-log-done~ (~#+STARTUP: logdone~ / ~lognotedone~) is covered by an 18-case state-transition matrix in progress-logging.test.js -- exactly the kind of decision logic that's easy to get subtly wrong: entering DONE with each setting, already-done-to-different-done-keyword correctly NOT re-triggering, leaving DONE unconditionally removing a stale CLOSED timestamp regardless of the CURRENT logging setting (cleaning up after an earlier session), and ~org-closed-keep-when-no-todo~'s carve-out applying only to the "cycled to no keyword at all" case specifically, never to cycling toward a different TODO keyword. The full app.js wiring (all three real TODO-transition call sites -- keyboard shortcut, tap-to-cycle badge, action-menu toggle -- confirmed to be the only mutation paths in the app before rewiring them) was additionally verified end to end through the real app: ~'time~ mode inserting and later removing a CLOSED timestamp, ~'note~ mode showing the prompt immediately without blocking the TODO badge's own instant state change, Save correctly filing the note under a timestamped Closing Note child heading, and Skip correctly declining to add one without reverting the state change that already happened.

** Footnotes and iCalendar export

Footnote support (~[fn:label]~ references, ~[fn:label:definition]~ / ~[fn::anonymous]~ inline definitions, and the separate-definition-line convention) is covered end to end: the inline parser (correct bracket-depth tracking so a definition containing its own ~[[link]]~ finds the right closing bracket rather than stopping at the link's own), the body parser's definition-line detection, ~findFootnoteDefinition~'s search across paragraphs/list items at any nesting depth/table cells (a real bug this coverage caught: nested list items are structured as nested list *nodes*, not a flat array, and the first version of this search didn't account for that), tap-to-jump navigation through the real app (a real bug this coverage caught: jumping to a definition only correctly re-expanded a *collapsed ancestor* of the target heading, not the target heading itself when the definition lived directly in its own body — ~navigateToHeading~'s ~revealOwnBody~ flag exists for exactly this and simply hadn't been passed), and export to both Markdown (GFM's own ~[^label]~ / ~[^label]: text~ syntax, including anonymous-footnote synthetic labeling and same-label deduplication) and HTML (jump-linked superscripts with a back-linked Footnotes section, including HTML-escaping verified specifically).

The iCalendar (.ics) exporter is covered for RFC 5545 conformance (line folding at 75 octets, CRLF endings, text escaping, UTC ~DTSTAMP~ vs. floating-time ~DTSTART~), correct ~RRULE~ generation from org repeaters (including that all three repeater marks produce identical output, matching this app's own agenda semantics), ~VALARM~ generation from a DEADLINE's delay/warning-period suffix, ~org-contacts-anniversaries~ becoming yearly-recurring events, and the same exclusion rules as the Agenda view (archived/commented/completed). Two real bugs this coverage caught: a UID-collision case where two headings sharing a title produced identical calendar-item IDs (fixed by folding the item's own date into the UID basis, chosen specifically to stay stable across re-exports rather than a position-based counter that would shift if an unrelated heading were inserted elsewhere in the file), and — found only through actual end-to-end UI testing, not the engine in isolation — the "this file + Agenda Files" export option silently producing an incomplete calendar if the user hadn't visited the Agenda view earlier in the session, since nothing had triggered the other files' fetches yet; fixed by making the export explicitly wait for every configured file to finish loading rather than exporting whatever happened to already be cached.
